package com.cvprado.clase_5;

public class ClaseAbstracta {

    Cuadrado cuadrado = new Cuadrado(4.7);
    Triangulo triangulo = new Triangulo(3.2,2.6);
    //Figura figura = new Figura() No se uede instanciar una clase abstracta
    }

    //Crear la clase Figura que tiene el atributo color y el método abstracto calcularArea()
    // Crear la clase Cuadrado que extienda de Figura. Esta clase tiene el atributo lado
    // Crear la clase Rectangulo que extienda de Figura. Esta clase tiene los atributos base y
    // altura
    // Crear la clase Triangulo que extienda de Figura. Esta clase tiene los atributos base y altura
    //Desde una clase que contenga main, verificar que Figura no se puede instanciar
}
